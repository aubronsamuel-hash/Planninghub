# Roadmap v1

## Foundation
Goal: establish core objects, time reservation, and conflict rules.

Steps:
1) Define core domain model and time reservation primitives.
   - Specs: docs/specs/core_domain_model.md, docs/specs/time_reservation_engine.md
2) Define minimal IAM and conflict engine contracts.
   - Specs: docs/specs/iam_minimal.md, docs/specs/conflict_engine_minimal.md

## Execution
Goal: minimal workflow from proposed to closed with timesheet capture.

Steps:
1) Define execution workflow states and timesheet contract.
   - Specs: docs/specs/execution_workflow_minimal.md (future)

## Intelligence
Goal: add matching scoring and automation after foundation and execution.

Steps:
1) Define matching score contract and inputs.
   - Specs: docs/specs/matching_minimal.md (future)

## Scale
Goal: multi org, marketplace, trust, and integrations.

Steps:
1) Define marketplace and trust contracts.
   - Specs: docs/specs/marketplace_minimal.md (future)
